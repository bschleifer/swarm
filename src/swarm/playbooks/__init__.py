"""Playbooks — self-improving procedural memory.

Generalizable procedures the headless Queen synthesizes from SUCCESSFUL
completed tasks, scored by real outcomes and (later phases) propagated
across the worker fleet.

DISTINCT from the ``skills`` table / ``SkillsStore`` (slash-command
workflow registry) and from Claude Code ``.claude/skills/`` artifacts —
see ``docs/specs/playbook-synthesis-loop.md`` (normative "Naming").
"""
